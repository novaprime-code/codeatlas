# CodeAtlas — Current Tasks

## Completed

### Sprint 0.1 — Repository Skeleton ✅

### Sprint 1.1 — Contracts ✅ (34 assertions)

### Sprint 1.2 — Core Engine ✅ (114 assertions)

### Sprint 2.1 — Scanner ✅ (59 assertions)

### Sprint 3.1 — Route Analyzer ✅ (79 assertions)

### Sprint 3.2 — JSON Exporter ✅ (57 assertions)

### Sprint 4.1 — Laravel Bridge ✅ (45 assertions)

### Sprint 4.2 — Web UI MVP ✅

- ✅ analysisStore + document loader with schema-version validation
  (rejects future majors, accepts newer minors, per JSON_SCHEMA.md versioning rules)
- ✅ LoadScreen: drag-and-drop + file picker with error reporting
- ✅ Sidebar: nodes grouped by type, counts, live search, collapse, select
- ✅ GraphCanvas: React Flow with custom AtlasNode (UI_GUIDELINES colors),
  deterministic columnar layout, minimap, select-to-center
- ✅ Placeholder-node synthesis: edge targets without real nodes (controllers,
  middleware — analyzers not built yet) render as ghosts derived from the
  {type}::{qualifier} ID convention — every edge always renderable
- ✅ Inspector: typed route detail view (URI, methods, name, controller,
  middleware, params, constraints, file) + connections list + generic fallback
- ✅ Types synced to ACTUAL backend output (nullable fields, absolute_path)
- ✅ UI fixture is REAL backend output (generated by the PHP pipeline, not handwritten)
- ✅ **All gates green: tsc clean · ESLint clean · 26/26 Vitest · vite build 371KB**

**THE SACRED PIPELINE IS COMPLETE, END TO END:**

```
Source → Scanner ✅ → AST Parser ✅ → Analyzer ✅ → DTO ✅ → JSON ✅ → UI ✅
```

A Laravel project can be scanned, analyzed, exported by `php artisan
codeatlas:analyze`, and the resulting document rendered as an interactive
architecture graph in the browser.

Cumulative backend assertions: 408. Frontend: 26 tests, 4 quality gates.

---

## Active: v0.1.0 Release Checklist

- ⬜ Nova: run Pest + PHPStan + Pint locally across all packages (composer install required)
- ⬜ Nova: run Testbench suite (packages/laravel)
- ⬜ Nova: end-to-end smoke on a real Laravel app: install bridge → analyze → load JSON in UI
- ⬜ Root README: screenshots/GIF of the UI, installation instructions
- ⬜ Tag v0.1.0, GitHub release notes from CHANGELOG
- ⬜ Packagist submission (contracts, core, scanner, analyzer-routes, exporter-json, laravel)

## Backlog (v0.2.0 — Sprint 5.1)

- Middleware analyzer (per ROADMAP Phase 5)
- Controller analyzer
- UI: replace synthesized controller ghosts with real controller nodes
